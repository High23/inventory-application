extends layout 

block content 
    h1= title 
    br
    if genres.length
        ul 
            each genre in genres
                li 
                    a(href=genre.url) #{genre.name}

    else 
        p There are no genres.